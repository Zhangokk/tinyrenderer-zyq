# 2026-06-19

## 今天做了

- 创建 TinyRenderer 
- 定义 `Color` 结构体
- 定义 `Image` 类
- 实现 `setPixel(x, y, color)`
- 输出第一张 `output/first.ppm`

## 今天理解的概念

- 软光栅渲染器可以先从“往图像缓冲区写像素”开始
- `Image` 本质上是一块 CPU 内存中的像素数组
- `setPixel` 负责把某个坐标的像素改成指定颜色
- PPM 是一种简单图片格式，适合作为渲染器早期输出

## 卡住的问题

-

## 明天任务

- 实现画线算法